rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Base segura: ningún documento es público.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
